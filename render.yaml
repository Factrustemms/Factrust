services:
  - type: web
    name: factrust
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    plan: free
    runtime: python
    pythonVersion: 3.10
